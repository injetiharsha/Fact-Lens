# Benchmarks

Benchmark runners and outputs for performance/quality checks.

## What Is Here
- `run_benchmark.py`: benchmark execution script.
- `results/`: raw benchmark outputs.
- `reports/`: summarized benchmark reports.

## Usage
Run benchmarks after model/config updates to compare regressions.
