# config (Legacy)

Legacy config folder kept for compatibility with older code paths.

## Recommendation
- Prefer `configs/` for current pipeline/training settings.
- Keep this folder only for backward compatibility during migration.
